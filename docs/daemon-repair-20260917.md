# Authorized unlaunched daemon repair

The user authorized continuing after protected failure 35156337705. Build completed; no candidate started and current stayed at a06a5921e2fa. Frozen state v3 revision 7 is bound by the exact digest in `production-maintenance-daemon-repair.mjs`.

Read-only diagnosis confirmed that only three virtual procfs identity components (inode, mtime, ctime) differ for PM2 PID 1932 on the original boot. PID, parent, start ticks, UID, executable, cwd and command digest remain identical. Add only this exact frozen tuple to the existing incident-specific historical comparison. Fresh observations remain exact and the PM2 socket peer, daemon environment and registry checks remain mandatory. No frozen runtime proof is rewritten.

The optional v3 `daemonRepair` audit retains the complete predecessor and original recovery. Only the exact never-launched failure can transition: revision +1, reviewed new target, held phase, immutable audit. Original deadline, token, ingress, runtime and database are preserved. Old schemas continue unchanged. All state writes still traverse the existing progress guard and byte/revision CAS under the operation lock; deployment flock also remains required.

Hosted workflow checks the exact signed backup/readiness, failed run, both main CIs, all ten jobs, no unknown production runs, exact source allowlist, fresh signed authority, quiet database and fixed 56-entry ledger. Host rechecks ingress/stopped runtime/database twice and archives the predecessor before CAS. No database change, service launch, current switch, or ingress opening occurs in recovery. Ordinary fresh backup/readiness/deploy/end remain required. Original and new failure evidence must never be relabelled as success.
